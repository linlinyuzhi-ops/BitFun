import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const sessionTitleNumberAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'session-title-number',
  parts: [{ id: 'root', propertyProfile: 'paint', visualRole: 'content' }],
};
