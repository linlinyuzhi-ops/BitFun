import React from 'react';
import EditorConfig from '@/infrastructure/config/components/EditorConfig';
import type { SettingsPageProps } from '../settingsTypes';

const EditorSettingsPage: React.FC<SettingsPageProps> = () => <EditorConfig />;

export default EditorSettingsPage;
