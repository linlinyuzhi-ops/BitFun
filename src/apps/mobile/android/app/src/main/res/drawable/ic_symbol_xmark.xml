<?xml version="1.0" encoding="utf-8"?>
<!--
    HarmonyOS `sys.symbol.xmark`, redrawn.

    Thinner and wider than Material's `Close`, which is what the source's close
    button actually draws inside its 42dp circle: the cross reaches almost the
    full width of the glyph box and its strokes are hairlines with round caps.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M4.6,4.6 L19.4,19.4"
        android:strokeWidth="1.6"
        android:strokeColor="#000000"
        android:strokeLineCap="round" />
    <path
        android:pathData="M19.4,4.6 L4.6,19.4"
        android:strokeWidth="1.6"
        android:strokeColor="#000000"
        android:strokeLineCap="round" />
</vector>
