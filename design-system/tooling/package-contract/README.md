# Package Contract

Private static checks that protect the design-system publication boundary:

- UI CSS variables must be generated by the system or theme token packages.
- UI source cannot import product applications, Tauri, stores, or locale catalogs.
- UI component CSS cannot contain raw color literals.
- Public package manifests can expose only built artifacts.
